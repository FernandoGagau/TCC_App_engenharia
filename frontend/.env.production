# Production environment variables
# Railway variables will be used if set, otherwise these defaults apply

# App configuration
REACT_APP_APP_NAME="Agente de Análise de Engenharia"
REACT_APP_VERSION="2.0.0"

# Build optimization
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

# Backend URL - MUST be HTTPS for production
# This is used as fallback if Railway variable is not set
REACT_APP_BACKEND_URL=https://backend-production-630c.up.railway.app

# Note: In Railway, set this variable directly (without ${{...}} syntax):
# REACT_APP_BACKEND_URL=https://backend-production-630c.up.railway.app
